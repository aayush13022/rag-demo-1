# Server-side backend URL for Next.js /api proxy rewrites (recommended on Vercel).
API_URL=http://localhost:8000

# Optional: call Railway directly from the browser instead of /api proxy.
# NEXT_PUBLIC_API_URL=http://localhost:8000
