# Recommended on Vercel (direct browser → Railway, best for long first request):
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# Fallback for /api server-side proxy (runtime, used when NEXT_PUBLIC_API_URL is unset):
API_URL=https://your-api.up.railway.app
